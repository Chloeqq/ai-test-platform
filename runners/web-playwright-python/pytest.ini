[pytest]
testpaths = tests
pythonpath = . tools
addopts = -v --output=test-results
markers =
    e2e: Browser tests requiring a live app or the in-repo login demo
    smoke: Curated smoke tests
    generated: AI-generated YAML-driven tests
