# Phase 5.1.4 代码卫生反方审计

日期：2026-07-07

## 1. 文件职责分析

### facade_test_point_assets.py — 1784 行，~25 个函数

| 函数 | 行数 | 职责 | 所属领域 | 应拆 |
|------|------|------|---------|------|
| batch_review_test_points | ~100 | Review + Gate check | Review + Quality | ⚠️ 双领域 |
| upsert_test_point_asset | ~110 | 编辑 + Snapshot | Asset + Snapshot | ⚠️ 双领域 |
| generate_cases_from_test_point_assets | ~190 | Generate + Gate check + 编译 | Generate + Quality | ⚠️ 三领域 |
| run_case | ~160 | Execute + Gate check | Execute + Quality | ⚠️ 双领域 |

4 个函数承担多领域职责。

### workbench_asset_views.py — 1150 行

Quality 代码占比 39%（9/23 函数），已形成事实子领域。

## 2. 硬编码清单

### Gate 错误码

- `quality_gate_blocked` (Generate/Execute) vs `gate_reject_blocked` (Review) — **不一致**
- `quality_gate_asset_missing` (Execute asset 缺失) — 独立语义

### Review 状态

"approved"/"pending"/"rejected" 散落 6 处，无 Enum。

### Gate 规则字符串

"assertion_missing"、"candidate_step"、"assert_" 在 asset_views.py 中多处重复。

`check_approve_point_gate` 独立实现规则，不复用 `get_quality_gate_violations`。

## 3. Quality Domain 分析

workbench_asset_views.py 中 9/23 函数为 quality 相关（39%）。已形成独立子领域，但与其他 view 函数混合在同一文件。

## 4. 重复逻辑

- Gate 规则在 `get_quality_gate_violations` 和 `check_approve_point_gate` 中重复实现
- Asset 文件读取在 4 个函数中独立实现

## 5. 异常码

`gate_reject_blocked` 应统一为 `quality_gate_blocked`。

## 6. 测试结构

已按领域自然分组：test_quality_gate_enforcement.py (17) + test_quality_report_warnings.py (13) + test_quality_snapshot.py (9) = 39 tests。

## 7. 重构优先级

- P0: check_approve_point_gate 统一 + 错误码统一
- P1: Review status 常量 + snapshot 下沉
- P2: facade 拆分 + asset 读取统一

## 8. 最小治理路线

Phase 5.1.5 (立即) → P0
Phase 5.2 → P1
Phase 6 → P2
