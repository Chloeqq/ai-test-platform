# Phase 5.1.5 Quality Domain 边界治理 — 审计+设计

日期：2026-07-07

## 一、当前代码问题清单

### 1.1 Gate 规则重复

`_has_assertion` 和 `_has_candidate` 检测逻辑在 `_build_quality_report` (lines 388-389) 和 `check_approve_point_gate` (lines 550-553) 中重复实现。

`check_approve_point_gate` (47 行) 独立实现 point 级 Gate 规则，不复用 `get_quality_gate_violations`。

### 1.2 错误码不统一

- `check_generate_gate`: `quality_gate_blocked`
- `check_approve_point_gate`: `gate_reject_blocked` (3 处)
- `check_execute_gate`: `quality_gate_blocked`

前端零引用 `gate_reject_blocked`，可直接统一。

### 1.3 Review status 散落

`"approved"` / `"pending"` / `"rejected"` 在 `facade_test_point_assets.py` 中 7 处硬编码。

### 1.4 Snapshot 字段常量

仅 1 处构造 + 测试读取，收益低。Phase 5.2 Dashboard API 自然形成 schema。不做。

## 二、最小修改方案

### P0-1: 抽取共享 point 级规则函数

新增 4 个 helper（workbench_asset_views.py 内部）：

```python
_point_has_assertion(point)       # 私有，2 处调用
_point_has_candidate_step(point)  # 私有，2 处调用
_point_has_assertion_missing_warning(point)  # 私有，2 处调用
get_point_gate_violations(point)  # 公开，check_approve_point_gate 调用
```

修改：
- `_build_quality_report`: lines 388-389 替换为 helper 调用
- `check_approve_point_gate`: ~47 行 → ~15 行，调用 `get_point_gate_violations`

### P0-2: 错误码统一

3 处 `"gate_reject_blocked"` → `"quality_gate_blocked"`，新增 `"scope"` 字段区分入口。

### P1: Review status 常量

新增 `REVIEW_STATUS_APPROVED` / `REVIEW_STATUS_PENDING` / `REVIEW_STATUS_REJECTED` 常量。存储格式不变。

### 不做：Snapshot 字段常量

过早抽象无收益。

## 三、影响文件

| 文件 | 改动 | 行数变化 |
|------|------|---------|
| workbench_asset_views.py | +4 helpers, 替换 2 处重复逻辑, 错误码统一 | +40 / -34 |
| facade_test_point_assets.py | review status 常量引用 (7 处) | 0 |
| test_quality_gate_enforcement.py | 错误码断言更新 | ~3 |

## 四、风险评估

- check_approve_point_gate 行为变化：零风险（相同 3 条规则）
- 错误码变更影响前端：零风险（前端无引用）
- Review 常量影响存储：零风险（存储仍为字符串）
- 已有测试：17 enforcement + 9 snapshot 预计全部通过

## 五、实现前审查（架构师视角）

### 可复用分析

**禁止重复实现：YES。**

| 现有实现 | 复用方式 |
|---------|---------|
| `_build_quality_report` lines 388-389 | 提取为独立 helper |
| `check_approve_point_gate` lines 550-553 | 替换为 helper 调用 |
| `check_generate_gate` / `check_execute_gate` | 已复用 `get_quality_gate_violations` |

### 模块化设计

不新建文件。4 个私有 helper + 1 个公开函数，保持 workbench_asset_views.py 内聚。

### 硬编码风险

`"assert_"` / `"candidate_step"` / `"assertion_missing"` 集中到各自的 helper 函数内。

### 多环境兼容性

不涉及。

### 最终结论

**是否允许实现：YES**

前置条件：
- 4 个 helper 提取，不改业务逻辑
- 错误码统一为 `quality_gate_blocked`
- 26 个测试全部保持通过
- Review status 常量化，存储格式不变

## 决定

- [ ] 实施 Phase 5.1.5
- [ ] 暂不实施
