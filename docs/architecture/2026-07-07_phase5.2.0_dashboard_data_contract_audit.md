# Phase 5.2.0 Quality Dashboard 数据契约审计

日期：2026-07-07

## 一、当前数据源状态

| 数据源 | 格式 | 记录数 | 字段数 | 覆盖 |
|--------|------|--------|--------|------|
| Snapshot JSONL | 逐行 JSON | 5 文件, ~10 行 | 14 字段 | 5/6 assets |
| Asset 实时 quality_report | 实时计算 | 6 assets | 14 字段 | 6/6 |
| Quality Gate violations | 实时计算 | — | 4 violations | 按 asset |
| Test Case DB | SQL | — | created_at, last_execution_result | 所有 cases |

## 二、Dashboard 指标可行性

| 指标 | 可行性 | 说明 |
|------|--------|------|
| 项目质量概览 | ✅ 可开发 | snapshot 聚合 + 实时 fallback |
| Asset 排名 | ✅ 可开发 | snapshot 最新行排序 |
| 问题分布 | ✅ 可开发 | 5 个 count 字段齐全 |
| 趋势分析 | ✅ 可开发 | JSONL 逐行读取, delta 可计算 |
| AI 质量分析 | ⚠️ 基础版 | source_type/trigger 维度, 缺 agent_id |

## 三、数据一致性风险

- v0 历史数据：2 files, filter `version > 0`
- 缺 snapshot 资产：1/6, 实时 quality_report fallback
- Asset 删除 → snapshot 孤儿：cross-check asset 文件

## 四、字段契约

必须保留：project, asset_id, version, at, score, decision, point_count, zero_assertion_count, candidate_step_count, unprocessed_count, quality_warning_count, data_warning_count, source_type, trigger

建议新增：previous_score, score_delta (P2, 可从相邻行计算)

暂不新增：agent_id, model_name, requirement_id

## 五、结论

允许进入 Phase 5.2 API 开发：YES
