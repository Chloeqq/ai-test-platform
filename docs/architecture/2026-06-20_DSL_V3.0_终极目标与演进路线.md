# DSL V3.0 终极目标与演进路线

> **最后更新：2026-06-25** | 当前 DSL 版本：V1.1 + V1.3 部分能力 | 状态：V1.2/V1.3 已上线管线
>
> 合并自 [2026-05-19 总体目标](../core/2026-05-19_DSL%20V3.0总体目标.md)（战略与路线）和 [2026-06-20 蓝图](dsl-v3.0-blueprint.md)（示例与差距），两份原文件已删除。

---

## 一、总体目标

V3.0 不是"换一种 YAML 写法"，而是建立一套**可追溯、可编译、可执行、可诊断**的企业级测试 DSL。

| 目标 | 含义 |
|------|------|
| 业务可读 | 测试人员看到的是意图、场景、数据、断言，而不是 CSS 和 XPath |
| 执行稳定 | 元素定位从页面对象库动态解析，优先使用真实 `data-testid` |
| 生成可控 | 每条用例必须绑定 `source_asset_id + intent_id`，避免重复生成 |
| 断言可信 | `expected_result` 从自然语言编译成可执行 assertion |
| 报告专业 | Allure 展示业务树、步骤、数据摘要、断言、证据和失败原因 |
| 批量可治理 | 执行前判断哪些用例高概率成功、哪些缺数据或缺前置 |

核心设计理念：**语义表达 → 编译治理 → 执行证据** 三者打通。

---

## 二、V3.0 目标用例（完整示例）

```json
{
  "dsl_version": "3.0",
  "case": {
    "id": "LOGIN_001",
    "name": "账号密码登录-密码错误校验",
    "domain": "user_center",
    "module": "login",
    "sub_module": "password_login",
    "priority": "P0",
    "risk_level": "HIGH",
    "tags": ["login", "security", "validation"]
  },
  "scenario": {
    "intent": "wrong_password_validation",
    "business_goal": "验证密码错误时系统的正确处理逻辑",
    "test_type": ["functional", "security"],
    "risk_points": ["暴力破解", "账号枚举"]
  },
  "actors": {
    "user": { "role": "normal_user" }
  },
  "environment": {
    "env": "test",
    "network_profile": "normal",
    "feature_flags": ["login_v2"]
  },
  "preconditions": [
    {
      "type": "user_exist",
      "user_ref": "user_001",
      "setup_action": "create_user",
      "params": {
        "username": "$test_data.user_001.username",
        "password": "$test_data.user_001.password",
        "status": "active"
      }
    }
  ],
  "test_data": {
    "user_001": {
      "username": "auto_user",
      "password": "CorrectPass123"
    },
    "input": {
      "username": "auto_user",
      "password": "WrongPass123"
    }
  },
  "business_flow": {
    "flow_id": "login_flow",
    "steps": ["input_username", "input_password", "click_login"]
  },
  "execution_plan": [
    {
      "id": "step_1",
      "action": "input",
      "target": { "page": "login_page", "locator": "username_input" },
      "value": "$test_data.input.username"
    },
    {
      "id": "step_2",
      "action": "input",
      "target": { "page": "login_page", "locator": "password_input" },
      "value": "$test_data.input.password"
    },
    {
      "id": "step_3",
      "action": "click",
      "target": { "page": "login_page", "locator": "login_button" }
    },
    {
      "id": "step_4",
      "action": "wait",
      "condition": {
        "type": "element_visible",
        "target": { "page": "login_page", "locator": "error_toast" }
      },
      "timeout_ms": 5000
    },
    {
      "id": "step_5",
      "action": "http_request",
      "method": "GET",
      "url": "/api/user/login-attempts?username=$test_data.input.username",
      "headers": { "Authorization": "Bearer $env.api_key" },
      "extract": { "fail_count": "$response.body.fail_count" }
    }
  ],
  "assertions": [
    {
      "id": "assert_http",
      "type": "api",
      "check": "response_code",
      "expected": 401,
      "on_fail": "continue"
    },
    {
      "id": "assert_ui_toast",
      "type": "ui",
      "check": "toast",
      "expected": "用户名或密码错误",
      "on_fail": "continue"
    },
    {
      "id": "assert_db_increment",
      "type": "db",
      "check": "login_fail_count",
      "params": { "username": "$test_data.input.username" },
      "expected_increment": 1,
      "on_fail": "continue"
    },
    {
      "id": "assert_user_status",
      "type": "business",
      "check": "user_status",
      "expected": "active",
      "on_fail": "abort"
    },
    {
      "id": "assert_no_enum",
      "type": "ai_semantic",
      "prompt": "分析以下提示语是否存在账号枚举风险：{toast_text}",
      "expected": "NO",
      "on_fail": "continue"
    },
    {
      "id": "composite_security",
      "type": "composite",
      "operator": "AND",
      "on_fail": "abort",
      "conditions": [
        { "type": "api", "check": "response_code", "expected": 401 },
        { "type": "ai_semantic", "prompt": "响应体是否泄露了用户是否存在的信息", "expected": "NO" }
      ]
    }
  ],
  "coverage": {
    "requirement_ids": ["REQ_LOGIN_001"],
    "risk_points": ["password_error", "account_security"]
  },
  "observability": {
    "logs": true,
    "trace": true,
    "screenshots": true,
    "video_recording": true
  },
  "rollback": [
    {
      "action": "reset_login_fail_count",
      "params": { "username": "$test_data.user_001.username" },
      "on": "always"
    },
    {
      "action": "delete_user",
      "params": { "username": "$test_data.user_001.username" },
      "on": "test_success"
    }
  ]
}
```

---

## 三、四层架构

V3.0 引入编译分层，解决"语义表达 vs 机器执行"的鸿沟：

```
第 1 层：Source DSL      面向测试人员，表达业务语义
        identity / requirement / scenario / data / steps / assertions / evidence

          ↓ DSL Compiler 编译

第 2 层：Execution IR    已解析但未执行的动作
        标准动作、页面、元素编码、数据引用、断言类型、traceability、compiler_status

          ↓ Runner Adapter 适配

第 3 层：Runner Script   兼容当前 runner 的具体脚本
        goto / input / click / assert_url / assert_visible + locator_type / locator_value

          ↓ Runtime Engine 执行

第 4 层：Runtime Record  执行后的事实记录
        实际 locator、耗时、截图、视频、console、失败 step、失败分类、Allure 标签
```

**关键原则**：Runner 不需要直接理解 `semantic_action`，只执行编译后的具体动作。当前 V1.1 直接运行在第三层（Runner Script），跳过了前两层的编译。

---

## 四、V1.1 → V3.0 差距分析

### 字段级对照

| 层次 | V1.1 现状 | V3.0 目标 | 新增内容 |
|------|----------|----------|---------|
| 元数据 | `project/module/title` | `+ domain/sub_module/risk_level` | 领域分类、风险等级 |
| 场景 | `intent_id/intent_type` | `+ business_goal/test_type[]/risk_points[]` | 业务目标、风控点 |
| 角色 | 无 | `actors + role` | 完全新能力 |
| 环境 | 硬编码 `page_url` | `env/network_profile/feature_flags` | 多环境支持 |
| 前置条件 | 纯文本字符串 | 结构化 `setup_action + params` | 可执行 setup |
| 变量 | `{{var}}` 单层 | `$test_data.xxx.yyy` 两级 | 变量解析引擎 |
| 步骤 | `input/click/goto/wait` | `+ http_request + extract + condition` | API 调用、变量提取 |
| 断言 | `assert_visible/text/url` (仅 UI) | api/ui/db/business/ai_semantic/composite (6 种) | 多维度断言 |
| 覆盖 | `requirement.source_asset_id` | `requirement_ids[]/risk_points[]` | 需求追溯 |
| 可观测 | 隐式（pytest 约定） | 声明式 `observability` | 声明式配置 |
| 回滚 | 无 | `rollback actions` | 完全新能力 |

### 关键设计约束

- **元素定位不发散**：DSL 中只写 `locator: "username_input"`，由页面对象库解析为具体定位策略
- **敏感数据脱敏**：密码、token 类字段标记 `sensitive: true`，报告只展示脱敏摘要
- **语义动作编译**：`semantic_action: input_username` 编译为 `action: input` + 页面对象绑定
- **语义断言映射**：`semantic_assertion: login_success` 必须配置具体断言组合（URL/用户名展示/token 等）
- **不修改被测系统**：`http://localhost:5174/#/login` 保留为业务事实，不自动篡改

---

## 五、分阶段演进路线

当前处于 V1.1。以下路线保持每一步都向前兼容，不一次性废弃现有用例。

| 阶段 | 目标 | 具体内容 | 状态 |
|------|------|---------|------|
| **V1.1** | 补齐身份追踪 | 所有生成用例写入 `source_asset_id + intent_id`；`project + source_asset_id + intent_id` 幂等键 | ✅ 已完成 |
| **V1.2** | 补齐可执行断言 | 质量门规则拦截弱断言用例（RULE_003 FATAL → REJECT，RULE_008 FATAL）；24条login用例拦截率75% | ✅ 已上线 (2026-06-24) |
| **V1.3** | 补齐结构化数据 | `subtype` 字段 (8种) + 自动推断 (`empty/whitespace/special_chars`) + 自然语言描述检测 + RULE_001 subtype增强匹配 | ✅ 已上线 (2026-06-25) |
| **V2.0** | 引入 Source DSL | 采用大结构：`identity/requirement/scenario/data/steps/assertions/execution/evidence`；仍编译成当前 runner 格式 | 未开始 |
| **V2.5** | 引入语义编译器 | `semantic_action` → 标准动作；`semantic_assertion` → 断言组合；`element` 动态绑定页面对象库；编译前能力检查 | 未开始 |
| **V3.0** | 智能治理型 DSL | 成功率预估、批量执行前门禁、失败自动归因、locator 自愈建议、运行证据反哺质量评分 | 未开始 |

---

## 六、与虚假通过问题的关系

当前 24 条 login 用例虚假通过根因分布（2026-06-25 批量验证结果）：

```
67% — 断言弱 (只有 assert_url)        → 质量门 RULE_003 FATAL → 已上线拦截  ✅
33% — 数据缺失 (data 字段不完整)       → 质量门 RULE_002 ERROR + V1.3 subtype 自动推断 ✅
~15% — 前置条件不可执行 (纯文本)       → V2.0 preconditions 引擎  ← 下一步
~10% — AI 幻觉/模板残留                → 质量门 RULE_016 (桩)
  4% — 步骤行为不匹配 (重复点击/超时)  → 质量门 RULE_011 WARNING ✅ 已上线
```

**更新**：V1.2 质量门 + V1.3 结构化数据已上线，当前 24 条拦截率 75%（18/24）。剩余 6 条 PASS 均为合法用例（边界值正向测试+功能性测试），1 条 REVIEW（前置条件不可执行）。

**DSL 升级主要解决前置条件结构化问题（~15%），断言和数据完整性问题（83%+）靠质量门规则更高效。**

---

## 七、成本估算

| 模块 | 对应阶段 | 预计工时 |
|------|---------|---------|
| 元数据/场景/角色/环境 | V2.0 | 2-3 周 |
| 前置条件引擎 (Setup Hook) | V2.0 | 3-4 周 |
| 变量系统升级 (`$xxx.yyy`) | V2.0 | 2-3 周 |
| API/DB 断言 | V2.5 | 3-4 周 |
| AI 语义断言 | V2.5 | 2-3 周 |
| 回滚引擎 | V3.0 | 2-3 周 |
| 智能治理（预估/归因/自愈） | V3.0 | 4-6 周 |
| **合计** | | **4-7 月（单人全职）** |

---

## 八、当前阶段建议

- ✅ **已完成 (2026-06-25)**：V1.2 质量门规则 11/17 上线、管线接入、24 条批量验证拦截率 75%；V1.3 subtype 自动标注 + 自然语言检测
- **当前优先**：`preconditions` 结构化（V2.0 前置条件引擎），解决 3 条用例的前置条件不可执行问题
- **并行推进**：质量门剩余 WARNING 规则（012-015）可在 V2.0 开发间隙补全
- **触发更大 DSL 升级的条件**：前置条件引擎落地后，剩余漏网用例的根因指向 DSL 其他结构性缺陷时

---

## 九、验收标准

- 新 DSL 不直接修改被测系统地址
- 同一 `source_asset_id + intent_id` 不再重复生成多个用例
- Runner 不需要直接理解 `semantic_action`，只执行编译后的具体动作
- 登录成功用例不只点击登录，必须有可执行断言
- Allure 报告展示业务层级、步骤、断言和脱敏数据
- 批量执行前能识别低成功率用例并给出原因（缺数据/缺前置/缺元素/缺断言/缺环境能力）
