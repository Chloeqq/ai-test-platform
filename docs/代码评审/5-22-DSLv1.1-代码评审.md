
[P1] apps/web-ui-service/app/api/workbench/facade.py:3128
run_case 仍然先要求 source_case_path 位于 assets/test-cases 且文件存在，然后才读取 DB 里的 script_code。这和“script_code 是唯一事实源、运行时再物化 YAML”的目标冲突。结果是：用例中心已有完整 script_code 时，只要本地 YAML 被删、未落盘或路径变动，执行仍会 404。建议改为：先按 case_id + project 找 DB 用例并取 script_code；只有 script_code 为空时，才校验并读取源 YAML 兜底。

[P1] apps/web-ui-service/app/services/test_case_data_service.py:69
normalize_test_steps() 新增了一些字段，但仍会丢弃 schema 已允许的 element_code / count / metric_rule / rule / source_point_key 等字段。schema 里允许 click/fill/input 用 element_code 代替 target，但服务端投影会把它过滤掉，导致 script_code -> test_steps -> TestCaseStep 投影不完整，页面展示和步骤表都会丢关键信息。建议让 normalize 白名单与 runner schema 对齐，至少补齐 schema 当前允许且执行/展示可能依赖的字段。

[P1] apps/web-ui-service/app/services/test_case_mapper.py:204
详情页映射优先读取 script_code 后，仍只按旧版 requirement: [string] 解析。现在 _workbench_yaml_script() 会把很多用例规范化为结构化 requirement: { intent_id, title, precondition, source_asset_id... }，这里 _list_value() 对 object 直接返回空，导致详情页无法展示结构化需求、前置条件和测试意图，退回用 test_steps_text 伪装 requirement。建议 mapper 复用 test_case_service._workbench_requirement() 或新增本地结构化 requirement 解析逻辑。

[P2] runners/web-playwright-python/runner/test_case_loader.py:67
指定 TEST_CASE_PATH 时使用 load_yaml_file()，而该函数不做 schema 校验；目录批量加载才会校验。第一阶段引入运行时 YAML 后，单用例执行正好走这个分支，意味着非法 YAML 可能绕过 schema，直到 executor 阶段才出现不可读的运行错误。建议对单文件路径也调用 validate_testcase_schema()，或者新增 load_validated_yaml_file()。

[P2] apps/web-ui-service/app/services/workbench_runtime_service.py:160
TEST_CASE_ALLOWED_ROOTS 直接加入 case_path.resolve().parent。在当前 facade 路径下通常安全，因为前面会校验或物化 runtime case；但 build_run_command() 自身没有约束调用方，未来其他入口如果传入任意 case_path，runner 就会把该目录加入白名单。建议只允许加入明确的 runtime case 根目录，例如 WEB_UI_RUNS_DIR/runtime-cases/{run_id}，不要泛化为任何 case_path.parent。

Open Questions
第一阶段是否要求“DB 有 script_code 就可以执行，不再依赖本地 YAML 文件存在”？如果是，run_case 当前逻辑需要调整。
requirement 结构化对象是否已经作为后续标准格式？如果是，mapper 和前端展示链路需要正式支持 object，而不是继续兼容式 fallback。